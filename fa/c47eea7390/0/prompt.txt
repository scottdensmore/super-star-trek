Work #171 following AGENTS.md.

---

<task-notification>
<task-id>a88e833a828754c0d</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-super-star-trek/8bd691e1-6038-4f0d-82d9-378514dcc470/tasks/a88e833a828754c0d.output</output-file>
<status>completed</status>
<summary>Agent "Run verifier on analyze change" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict: pass

No failures. One thing you should know first: `tests/analyze.sh` changed on disk at 09:03:40, partway through my session — blob `e6c8fc2` → `8eeb263`, +7 lines. The change is comment-only (a new paragraph documenting the two mutation proofs, and "30 is the deepest of those with headroom over it" reworded to "30 clears the deepest of those with room over it"); the eleven non-comment added lines are identical in both. Everything I executed ran against the current state, and I re-ran `analyze` in both CI configs afterwards to make that claim without qualification.

## What ran

Builds (clean rebuilds, `--clean-first`, so every TU met `-Werror`):
- `cmake --preset ci-debug &amp;&amp; cmake --build --preset ci-debug --clean-first` — 13 sst sources + 2 test binaries, **zero warnings**, exit 0.
- `cmake --preset ci-release &amp;&amp; cmake --build --preset ci-release --clean-first` — same, **zero warnings**, exit 0.

Tests — full suite, both configs, 12/12 Passed each:
- `ctest --preset ci-debug` — 352.47s total.
- `ctest --preset ci-release` — 340.65s total.
- `tui` ran (tmux present) rather than skipping: 174.25s Debug, 162.42s Release.

`analyze` timings (your specific ask), six runs, all Passed:

| run | Debug | Release |
| --- | --- | --- |
| isolated `-R '^analyze$'` | 35.37s | 33.46s |
| inside full suite | 32.48s | 32.73s |
| isolated, re-run post-edit | 33.07s | 31.32s |

`TIMEOUT 300` against a 31–35s spread is roughly a 9x margin — sane, and the CMakeLists comment's reasoning holds.

Journeys: piped plain-mode journey against `./build/ci-debug/sst` and `./build/ci-release/sst` (`regular/short/novice/xyz/srscan/status/quit/n`) — exit 0 both, no stray CRs in the Debug transcript, normal sign-off. No game source changed, so this is a formality; `tui`, `golden`, `tournament`, `journey`, `journey-tui` all passed above.

`shellcheck tests/analyze.sh` and `shellcheck -s sh tests/analyze.sh` — both clean, against the current file. `/bin/sh` here is dash, which is what the shebang gets under ctest.

## Independent checks of the change's claims

I re-derived the load-bearing measurements rather than taking them from your report. All on gcc 15.2.0 (`/usr/bin/cc`), with the exact flag string CMake passes.

- **The premise holds.** Planting an analyzer-only uninitialized read at the top of `cramen()` in a copy of `sst.c` outside the repo: default depth reports nothing and exits 0; `--param=analyzer-bb-explosion-factor=30` fails with `error: use of uninitialized value 'uninit_probe' [-Werror=analyzer-use-of-uninitialized-value]`.
- **The fallback leg works.** Driven with a shim on `$PATH` that rejects the `--param` the way an older gcc would. Output: `NOTE: .../oldgcc does not analyse with --param=analyzer-bb-explosion-factor=30; using its default depth`, then `analyze OK (.../oldgcc, bb-explosion-factor default)`, exit 0, 15.8s.
- **`-Wanalyzer-too-complex` counts are exact.** At the default factor I get battle.c 169, setup.c 142, sst.c 46, and 0 in exactly five of the thirteen (finish.c, osx.c, planets.c, rules.c, tuifmt.c). At factor 30, sst.c gives 298. Both match the comment to the digit.
- **Timings and the zero-diagnostic claim are exact.** 15.8s at default, 32.9s at 30, 47s at 60 over the thirteen sources; zero diagnostics at every one, and zero under the Release flag set (the passing `ci-release` `analyze` confirms that leg).
- **"Thirteen sources"** matches the sst target's source list in `build/ci-debug/CTestTestfile.cmake`.
- The passing line reads `analyze OK (/usr/bin/cc, bb-explosion-factor 30)`.

## Missing coverage

**A silent fallback on Linux CI is not a failure, and would be invisible.** The script already treats "no analyzer-capable compiler on Linux CI" as a hard failure, on the reasoning that GCC is guaranteed there. The new depth probe has no matching rule: a compiler that is found but rejects the `--param` falls back to default depth, prints the `NOTE:` to stderr, and exits 0 — on Linux CI too. Since #171's whole premise is that the default depth misses `cramen()`, `cloak()` and `tui_shutdown()`, that path reverts the check to precisely the reach this change exists to fix, and nothing goes red. ctest prints nothing for a passing test, so the `NOTE:` never reaches a CI log a human reads. The asymmetry is at `/home/scottdensmore/Developer/scottdensmore/super-star-trek/tests/analyze.sh:173-179` (the CI-fails-on-skip gate) versus `:192-196` (the fallback, which has no such gate). Your call whether that belongs in this slice or #171's remaining half; I am reporting it, not asking for it.

Nothing else. The change is a test script and a timeout, both exercised directly by `ctest`, and no game source moved.

## Two comment nits

Neither is actionable as a failure; both are the kind of stated number AGENTS.md holds to specification standard.

- **`tests/analyze.sh:39` — "cramen() at 25 and not at 24" is probe-dependent and not reproducible from the comment.** My own probe (`static volatile int vz; int uninit_probe; if (vz) uninit_probe = 1; if (uninit_probe) vz = 2;` at the top of `cramen()`, which matches the comment's description of "conditional on a volatile") is missed at 8 and caught at 10 — not 24/25. The material claim is unaffected: default (5) misses, 30 catches, with headroom either way. But a reader who plants their own read to check the boundary will get a different number and think the comment wrong, because the exact statement planted decides how many paths the analyzer has to walk. The comment gives the shape but not the text.
- **`CMakeLists.txt:203-204` — the arithmetic does not close on the stated numbers.** "the run measured 15s here before and 32s after, so what was eleven times the measurement is now nine": 180/15 is 12, not 11. It works against the unrounded figure (180/15.8 = 11.4), which is what I measured, so it is the rounding in the sentence rather than the reasoning.

## Environment issues

None that blocked a check. `cppcheck`, `clang-tidy` and `scan-build` are all absent — per `verifier.md` step 2 that is no longer a gap, since `analyze` is what step 2 rests on and it ran (not SKIPPED) in both configurations. tmux is present, so `tui` ran rather than skipping.

## What I did not repeat, and whether I think it was wrong

Your `ctest --preset debug -R '^analyze$'` and `shellcheck -s sh` I re-ran anyway (the shellcheck against the newer file, the analyze under the `ci-*` presets instead) — no disagreement. Your mutation proof and fallback proof I reproduced by different means and got the same answers, so I have no quarrel with either. The one number I could not reproduce is the 24/25 boundary above.

No files were modified by this pass; `git status` still shows only `CMakeLists.txt` and `tests/analyze.sh` modified, nothing staged, nothing untracked. All my scratch work — the mutated `sst.c` copy and the compiler shim — lives outside the repository.</result>
<usage><subagent_tokens>57183</subagent_tokens><tool_uses>37</tool_uses><duration_ms>1370103</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a5e2f5ec8698ba1ff</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-super-star-trek/8bd691e1-6038-4f0d-82d9-378514dcc470/tasks/a5e2f5ec8698ba1ff.output</output-file>
<status>completed</status>
<summary>Agent "Rerun verifier after fixes" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict

**Pass.** Both `ci-*` presets build clean with `-Werror`, both full test suites are 12/12 green, the new depth gate is proven on both branches, and — the thing you most wanted checked — the gate provably cannot fire on the gcc CI actually runs.

## The CI question, settled empirically

CI's Linux compiler is **GCC 13.3.0 on `ubuntu-24.04`** (`gh run view 32040760714 --log`: `Image: ubuntu-24.04`, `-- The C compiler identification is GNU 13.3.0`; macOS is AppleClang 21.0.0 and reports `analyze` Skipped, as designed).

I ran the repository's own `/home/scottdensmore/Developer/scottdensmore/super-star-trek/tests/analyze.sh`, mounted read-only, inside an `ubuntu:24.04` container whose gcc is `13.3.0-6ubuntu2~24.04.1` — the same distro package the runner has — with `CI=1` and the exact flag string and source list from `build/ci-debug/CTestTestfile.cmake`:

```
gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
analyze OK (/usr/bin/cc, bb-explosion-factor 30)
analyze.sh exit=0 after 33s
```

So on CI's compiler the `--param` is accepted, the canary still reports at depth 30, the gate does not fire, and there is no fallback. I also ran the 13-source loop at factor 30 under **both** flag strings CMake passes (Debug with `-DDEBUG`, Release without) on that gcc 13.3: **zero diagnostics**, 30s and 32s. So CI will not redden either from a rejected param or from new findings on a compiler two majors older than the one the comment was measured against. Bracketing: gcc 13.3 clean and gcc 15.2.0 clean (the local ctest runs) — gcc 14 untested, which is the only version a future `ubuntu-latest` bump could land on that nobody has looked at.

Timeout headroom is ample: CI's runner is comparable-to-slightly-faster than this machine on this suite (`tui` 155.85s there vs 174.37s here; `eof` 18.96 vs 19.26), and factor 30 costs 30–33s here, against `TIMEOUT 300` and a job currently finishing in 5m53s of a 15-minute budget.

One unexplained curiosity, not a finding: CI's `analyze` at default depth took **5.28s/5.44s**, where the same gcc 13.3 over the same sources takes **17s** here — while a plain non-analyzer compile of those 13 sources is 2s here vs ~1.3s on CI, so raw compile speed differs only ~1.5x. The 3x gap is on the analyzer leg specifically. It does not change the verdict (every measurement is far inside the timeout), but it means the cost and reach numbers in the comment may not predict CI's; the comment already scopes them to "this machine", so I am not raising it as a claim to fix.

## Re-verification of the three items

1. **Gate (was my coverage finding).** Independently re-proved with my own shim forwarding to `/usr/bin/gcc` but rejecting the param, run against the real tree: `CI=1` → `FAIL: ... does not analyse with --param=analyzer-bb-explosion-factor=30, so Linux CI would have run at gcc's default depth`, exit 1. `CI` unset → `NOTE: ...` then `analyze OK (..., bb-explosion-factor default)`, exit 0. The silent-fallback hole is genuinely closed.
2. **The 25/24 cliff.** Reproduced exactly, on a copy of the working tree outside the repository with the comment's literal statement planted at the top of `cramen()`: gcc 15.2.0 gives 0 diagnostics at 5, 0 at **24**, 2 at **25**, 2 at 30. Plain `-Wall -Wextra` without `-fanalyzer`: 0 — so the catch is the analyzer leg, as claimed. Running `tests/analyze.sh` from inside that copy fails: `FAIL: sst.c did not analyse clean` / `error: use of uninitialized value 'u' [-Werror=analyzer-use-of-uninitialized-value]`, exit 1. And CI's gcc 13.3 misses the same read at 5 and catches it at 30, so the reach the change buys is real on the compiler CI uses, not only on gcc 15.
3. **The arithmetic.** `180/16 → 11` and `300/32 → 9` both close now. See the nit below.

## Findings

**Nit (documentation, non-blocking): the two files state different numbers for the same measurement.** `tests/analyze.sh:64` says "15s at the default"; `CMakeLists.txt` now says "the run measured 16s here before and 32s after". I measured the pre-change script (`git show HEAD:tests/analyze.sh`) over the 13 sources at **15.38s**, and the new script's fallback path at **15.00s / 15.24s** on two runs; ctest overhead is ~0.3s (ctest reported `analyze` at 32.27s where the direct factor-30 run is ~32s). So 15s is the figure that reproduces and 16s is ~0.6s high — at 15s, "eleven times the measurement" is nearer twelve. Both sentences cite the same run, so a later reader cannot tell which is the measurement. The conclusion (300 is right) is unaffected either way.

No failures, no flakes, no missing coverage. The mutation proof for the new depth exists and I reproduced it; the fallback and the gate each have a proof and I reproduced both.

## Environment

- `cppcheck`, `clang-tidy`, `scan-build`: not installed (bonus only — step 2 no longer rests on them). `shellcheck` 0.11.0 and `tmux` 3.6 present.
- `analyze` did **not** skip anywhere in my runs: it ran with local gcc 15.2.0 at factor 30 in both configurations, so static analysis genuinely ran. Its depth remains uneven per function — not a tree-wide clean bill.
- `tournamen.trk` sits in the repository root (Aug 16, gitignored since 91fbe91). Pre-existing, not from this diff.

## What ran

Working tree state: branch `test/analyze-explosion-factor` has **no commits ahead of `main`**; the change is entirely unstaged (`M CMakeLists.txt`, `M tests/analyze.sh`), nothing staged or untracked, no game source touched. Nothing in the repository was modified by this pass.

- `cmake --preset ci-debug &amp;&amp; cmake --build --preset ci-debug --clean-first` — exit 0, zero warnings/errors.
- `cmake --preset ci-release &amp;&amp; cmake --build --preset ci-release --clean-first` — exit 0, zero warnings/errors.
- `ctest --preset ci-debug` — 12/12 passed, 357.24s (`tui` 174.37s, `analyze` 32.27s).
- `ctest --preset ci-release` — 12/12 passed, 350.06s (`tui` 171.61s, `analyze` 30.80s).
- `shellcheck -s sh tests/analyze.sh` — clean.
- Depth/gate probes described above (docker `ubuntu:24.04` + `gcc:13`, shim compiler, planted-read copy under `/tmp/claude-1000/.../scratchpad/copy`).
- Journeys: `printf 'regular\nshort\nnovice\nxyz\nsrscan\nstatus\nlrscan\nquit\ny\n'` piped to `build/ci-debug/sst` and `build/ci-release/sst` — exit 0 both, 77 lines each, no carriage returns, clean sign-off. TUI journeys covered by the `tui` test in both configurations.

**On scope, since you asked me to say rather than run out of duty:** I ran the full battery, but honestly, only part of it could have been affected. No game source, build flag or preset changed, so `golden`, `tournament`, `eof`, `help`, `tui`, `journey*`, `tuifmt`, `rules` and `score` were controls, not tests of this delta. The delta's own coverage is: both `ci-*` builds, `ctest -R '^analyze$'` in both configurations, and the gate/depth probes above. I ran the rest anyway because it costs wall time rather than context and gives you a green baseline for the commit — but a narrower rerun of just those would have been defensible, and is what I would run again if only the comment text changed.</result>
<usage><subagent_tokens>72684</subagent_tokens><tool_uses>42</tool_uses><duration_ms>1302858</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a34d7b6efcbb010c3</task-id>
<tool-use-id>toolu_01H2BZiBxSB6mLMsMFLbD2E1</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-super-star-trek/8bd691e1-6038-4f0d-82d9-378514dcc470/tasks/a34d7b6efcbb010c3.output</output-file>
<status>completed</status>
<summary>Agent "Code review the analyze change" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict

**Approve** — for the exact worktree state I reviewed: branch `test/analyze-explosion-factor`, no commits yet, base `main` at `2ab86ff`, two modified files (`/home/scottdensmore/Developer/scottdensmore/super-star-trek/tests/analyze.sh`, `/home/scottdensmore/Developer/scottdensmore/super-star-trek/CMakeLists.txt`), nothing staged, nothing untracked. Substance: raise `analyzer-bb-explosion-factor` to 30 behind a probe with a Linux-CI gate, print the factor on the pass line, raise the ctest TIMEOUT 180→300, and record the measurements in comments.

No behavioral defect found. Everything I could check independently checked out — see "What I verified" below. Findings 1 and 2 are comment text; taking finding 1 changes the state, so AGENTS.md step 11 formally asks for another round, and I'd accept the changed state on the strength of this review if the only delta is the comment wording proposed there plus a re-run of `shellcheck -s sh` and `sh -n`.

## Findings

### should-fix

**1. `tests/analyze.sh:265-266` — the SC2086 justification now covers two unquoted expansions and names only one; the invariant that `$depth` must stay unquoted is nowhere stated.**

```sh
	# shellcheck disable=SC2086  # $flags is a flag list, not one word
	if ! "$cc" -fanalyzer $depth $flags -Werror -c -o "$work/obj.o" "$src" \
```

`$depth` is unquoted for a different reason than `$flags`: it is `""` on the fallback path, and an unquoted empty expansion contributes zero words where a quoted one contributes one empty argument. Nothing on the line says that, and the probe 45 lines up writes `"$depth"` quoted (line 220) — so the file reads as an inconsistency a later editor is invited to "fix". Failure scenario, confirmed here: with `$depth` quoted and empty, gcc receives an empty argument and answers `gcc: error: : linker input file not found: No such file or directory`, so all thirteen sources report `FAIL: … did not analyse clean` and the test fails for a reason that has nothing to do with the game or the analyzer. Suggested fix — extend the existing comment rather than adding a paragraph, e.g. `# $flags is a flag list, not one word; $depth is empty on the fallback path, where quoting it would pass one empty argument instead of none`.

### nit

**2. `tests/analyze.sh:223-229` — the reason given for treating non-CI machines differently rests on visibility that ctest denies there too.** The gate's comment argues that on Linux CI the note "would reach nobody, since ctest prints nothing at all for a test that passes", while "everywhere else a fallback to gcc's default depth is a weaker check saying so on the line it prints" — but under `ctest --preset debug` the local developer sees exactly the same nothing: stdout and stderr of a passing test are both swallowed, and the presets' `--output-on-failure` only fires on a failure. So on a machine whose gcc lacked the param, `ctest` would print `analyze … Passed` and the developer would have the weaker check with no signal. Low stakes, because the branch is close to unreachable — `analyzer-bb-explosion-factor` has been documented since the analyzer landed in gcc 10, so every compiler that passes the canary should accept it — which is why this is a nit rather than a request to change the gate. Suggested fix: say that the fallback is visible only when the script is run directly or under `ctest -V`, so the asymmetry with the CI branch is stated rather than implied.

**3. `tests/analyze.sh:64` and `CMakeLists.txt:203` — "15s" is the fast end of a noisy range, and the derived "twelve times" is the most flattering reading of it.** Three runs of the pre-change script here with the Debug analyze flags gave 16.85s, 16.30s and 15.42s; the post-change script gave 32.92s and `ctest --preset debug -R '^analyze$'` reported 31.42s. `47s at 60` measured 45.74s. Nothing in the conclusion moves — 180/16.3 ≈ 11 rather than 12, and the margin argument holds either way — but this repo treats a number in a comment as specification, and a single sample stated as a flat figure is the kind of claim a later reader cannot reproduce. Suggested fix: "about 16s (15.4–16.9s over three runs)" and "about 46s", or say the figures are single runs.

**4. `CMakeLists.txt:202-206` — the timeout comment does not name what it is coupled to.** It says "the analyzer depth the script now asks for" without naming `factor=30` in `tests/analyze.sh`, so a later raise to, say, 60 (about 46s here, and 47s is already recorded in the script as measured) leaves "32s after" and the 300 silently stale with nothing greppable connecting them. Suggested fix: name `tests/analyze.sh`'s `factor=30` in the comment.

## On the passes

- **`ui-review` skip: I agree, and it is not close.** The diff touches no game source, adds and removes no compile define reaching `sst`, changes no preset or build type, and does not alter `analyze_flags`. The only new output is a test script's own pass line and a ctest property. Nothing here can change what the game shows a player or asks of them. Recorded so the skip is on the record with a second opinion behind it.
- **`verifier`:** two runs, the second seeing everything but comment text, with `shellcheck -s sh`, `sh -n` and `ctest -R '^analyze$'` re-run over the final state. Adequate — the post-verifier delta is comment-only, and the two checks that a comment edit can actually break were re-run. I independently confirmed `shellcheck -s sh` clean, `sh -n`/`dash -n` clean, and `ctest --preset debug -R '^analyze$'` Passed (31.42s).
- Both fallback proofs reproduce under dash with a shim rejecting the `--param`: no `CI` gives `NOTE: … using its default depth` then `analyze OK (…, bb-explosion-factor default)` and exit 0; `CI=1` gives the FAIL and exit 1.

## What I verified independently (gcc 15.2.0 here, dash as `/bin/sh`)

Every measured claim in the new comment text reproduced exactly, on a copy of the tree outside the repository:

- The planted read `{ volatile int c = 0; int u; if (c) u = 1; if (u == 42) c = 2; }` — `cramen()` caught at 25 and not at 24; `cloak()` at 7 and not at 6; `tui_shutdown()` at 8; `newqad()` and `phasers()` caught at gcc's default; invisible to `-Wall -Wextra` at `-O0` in both files, so the catch is the analyzer leg.
- `-Wanalyzer-too-complex` at the default: `battle.c` 169, `setup.c` 142, `sst.c` 46, and exactly five of the thirteen at 0 (`finish.c`, `osx.c`, `planets.c`, `rules.c`, `tuifmt.c`). `sst.c` at 30: 298.
- Clean at 30 under both configurations' flags here, and — in an `ubuntu:24.04` container, gcc 13.3.0, which is what CI carries — the `--param` is accepted and the tree is clean at 30 under both `-DDEBUG` and Release flags. That corroborates the second verifier's container result rather than taking it on report.
- "CI's own analyze leg runs faster than this one": CI run 32040760714 reports `analyze … Passed 5.28 sec` (ci-release) and `5.44 sec` (ci-debug) on ubuntu-latest against 15–17s here, so the claim holds and the 300s timeout has enormous headroom there.
- Scope: only the two files, no debug leftovers, no stray artifacts. `verifier.md:52-54` ("its depth is uneven … says nothing where it gives up") stays true after the change — the too-complex count rises at 30 — and #171 stays open per the script's own comment, so no doc update is owed.

## Not reviewed

- Nothing material. I did not re-derive the rejected-snippet aside at `tests/analyze.sh:44-47` ("the analyzer does not flag a read into a volatile at all"); it justifies a discarded alternative and is not load-bearing. I confirmed the other half of that sentence (an unconditional read is flagged by plain `-Wuninitialized`).
- I did not run the full `ctest` battery or the `ci-*` presets — that is the verifier's scope, and it reported both green.</result>
<usage><subagent_tokens>67421</subagent_tokens><tool_uses>39</tool_uses><duration_ms>920235</duration_ms></usage>
</task-notification>